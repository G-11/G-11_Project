コード規約

記法
混合キャメルケース
関数名とメンバ変数はツインキャメル 例)CastMatrix(void)(マトリクス再計算)
ローカル変数はロワーキャメル 例) scaleMatrix(スケール行列)

読み難くなるようならスネークも混ぜる

基本的に 動詞+名詞 な感じ

ゲッターが存在するメンバー変数は 最初に＿をつける 例)_Size
その場合のゲッターは　Size(void)const となる。

引数について…
構造体を引数にする場合は参照渡し SetPos(const D3DXVECTOR3& pos);

クラス名は予約語に登録されていなければ名前 Sprite(2Dポリゴン)
されていたら頭文字にCをつける (CPolygon)